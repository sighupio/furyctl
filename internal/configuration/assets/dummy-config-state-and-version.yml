kind: Bootstrap
metadata:
  name: my-dummy
executor:
  version: "0.12.12"
  state:
    backend: s3
    config:
      bucket: "im-fury"
      key: "demo"
      region: "eu-milan-1"
spec:
  provisioner: dummy

  rsaBits: 4096
